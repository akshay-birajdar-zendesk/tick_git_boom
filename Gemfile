source 'https://rubygems.org'

gem 'cli-kit', '~> 5.2.0'
gem 'cli-ui', '~> 2.7.0'

# cli-ui's prompt requires reline; it stops being a default gem in Ruby 3.5.
gem 'reline'

group :test do
  gem 'mocha', '~> 2.4', require: false
  gem 'minitest', '>= 5.0.0', require: false
  gem 'minitest-reporters', require: false
end
