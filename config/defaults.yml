# Default configuration for tickgitboom.
# The runtime tickets file lives outside version control; on first boot the
# tracked seed is copied into place, and `tickgitboom boom` restores it.
format: auto
data_path: data/tickets.json
seed_path: seeds/tickets.json
